<h1> Resultado da Busca: <%= @query %> </h1>
<hr/>
<% if @insurances.empty? %>
  <p>Nenhuma Seguradora encontrada</p>
<% end %>
<% @insurances.each do |insurance| %>
  <div>
    <dl>
      <dt> <%= t(:name)%>:</dt>
      <dd> <%= link_to insurance.name, insurance_path(insurance.id) %></dd>
      <dt> <%= t(:insurance_name)%>:</dt>
      <dd> <%= insurance.insurance_name %></dd>
      <dt> <%= t(:price)%>:</dt>
      <dd> <%= number_to_currency(insurance.price) %></dd>
    </dl>
  </div>
<% end %>

